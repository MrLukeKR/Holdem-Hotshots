using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTestProject1
{
    [TestClass]
    public class Player
    {
        [TestMethod]
        public void giveChipTest()
        {
            Player player = new Player("Billy", new Socket(SocketType.Stream, ProtocolType.Tcp);
        }
    }
}
